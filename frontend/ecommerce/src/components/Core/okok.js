{
  /* <React.Fragment>
<table class="table table-dark">
  
  <tbody>
  <Link
  to={`/product/${product._id}`}
  className="mr-2"
  style={{ textDecoration: "none" }}
>
    <tr>
      <th scope="row">{product.name}</th>
      {/* <td><DisplayImage item={product} url="product" /></td> */
}
//       <td> {product.price} Rwf</td>
//       <td> {showStock(product.quantity)}</td>
//       <td>{showRemoveButton(showRemoveProductButton)}</td>
//       <td> {showCartUpdateOptions(cartUpdate)}</td>
//     </tr>
//     </Link>
//     {showAddToCartBtn(showAddToCartButton)}
//   </tbody>
// </table>
// </React.Fragment> */}

{
  /* <table class="table table-dark">
  
  <tbody>
  <Link
  to={`/product/${product._id}`}
  className="mr-2"
  style={{ textDecoration: "none" }}
>
    <tr>
      <th scope="row">{product.name}</th>
      {/* <td><DisplayImage item={product} url="product" /></td> */
}
//       <td> {product.price} Rwf</td>
//       <td> {showStock(product.quantity)}</td>
//       <td>{showRemoveButton(showRemoveProductButton)}</td>
//       <td> {showCartUpdateOptions(cartUpdate)}</td>
//     </tr>
//     </Link>
//     {showAddToCartBtn(showAddToCartButton)}
//   </tbody>
// </table>
// </React.Fragment>

//  */}

{
  /* <React.Fragment>
<Link
  to={`/product/${product._id}`}
  className="mr-2"
  style={{ textDecoration: "none" }}
>
  <div className="card shadow">
    <div className="inner">
      <DisplayImage item={product} url="product" />
    </div>

    <div className="card-body text-center">
      {shouldRedirect(redirect)}

      <p className="" style={{ color: "#000000" }}>
        {product.price} Rwf {product.name}
      </p>
      {showStock(product.quantity)}
      <br />

      {showAddToCartBtn(showAddToCartButton)}

      {showRemoveButton(showRemoveProductButton)}

      {showCartUpdateOptions(cartUpdate)}
    </div>
  </div>
</Link>
</React.Fragment> */
}
